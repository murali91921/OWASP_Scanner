using System;
using System.IO;
using System.Net.Http;
using System.Net.Http.Headers;
using System.Web;

namespace CFSNSExample2
{
    class CFSClassExample2
    {
        /*
        //Snippet 1
        public HttpResponseMessage Snippet1()
        {
            var resp = new HttpResponseMessage();
            CookieHeaderValue cookie = new CookieHeaderValue("TestCookie", "12345");
            CookieHeaderValue cookie1 = new CookieHeaderValue("TestCookie", "12345");
            cookie.Expires = DateTimeOffset.Now.AddDays(1);

            cookie.Path = "/";
            cookie.Secure = true;
            resp.Headers.AddCookies(new CookieHeaderValue[] { cookie });
            return resp;
        }

        //Snippet 2
        public void Snippet2(HttpResponse Response)
        {
            Response.Cookies.Add(
                new System.Web.HttpCookie("key2", "value2")
                {
                    Secure = true,
                });
        }

        //Snippet 21
        public void Snippet2(HttpResponse Response)
        {
            Response.Cookies.Add(
                new HttpCookie("key21", "value21")
                {
                    Secure = true,
                });
        }

        //Snippet 3
        public void Snippet3(System.Web.HttpResponse Response)
        {
            if (Response.Cookies.Count > 0)
            {
                foreach (string s in Response.Cookies.AllKeys)
                {
                    if (!Response.Cookies[s].Secure)
                    {
                        Response.Cookies[s].Secure = true;
                    }
                }
            }
        }

        //Snippet4
        /*public void Snippet4()
        {
            var validationCookie = new CookieHeaderValue("cookieName_Snippet4", "cookieValue_Snippet4")
            {
                Path = "/",
                HttpOnly = true,
                Secure = true
            };
        }*/

        //Snippet5
        public void Snippet5()
        {
            HttpCookie httpCookie;
            httpCookie = new HttpCookie("cookieName_Snippet5", "cookieValue_Snippet5")
            {
                Path = "/",
                HttpOnly = true,
                Secure = true
            };
        }*/
    }
}